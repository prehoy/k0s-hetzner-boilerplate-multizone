# Copy to secrets.yml (gitignored) — ideally encrypt with `ansible-vault encrypt secrets.yml`.
keepalived_password: CHANGEME_lb_keepalived   # VRRP auth_pass, shared by the LB pair
hetzner_api_token: CHANGEME_hetzner_token     # used by failover.sh for the API VIP + NAT-route failover
