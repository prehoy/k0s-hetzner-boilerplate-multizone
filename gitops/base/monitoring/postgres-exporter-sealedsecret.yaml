# PLACEHOLDER SealedSecret — contains NO real secret.
# Reseal with seal.sh / kubeseal against your own sealing key before applying.
---
apiVersion: bitnami.com/v1alpha1
kind: SealedSecret
metadata:
  name: postgres-exporter-secrets
  namespace: monitoring
spec:
  encryptedData:
    DATA_SOURCE_NAME: AgPLACEHOLDER_RESEAL_ME_no_real_secret_here_0000000000
  template:
    metadata:
      name: postgres-exporter-secrets
      namespace: monitoring
