# PLACEHOLDER SealedSecret — contains NO real secret.
# Reseal with seal.sh / kubeseal against your own sealing key before applying.
---
apiVersion: bitnami.com/v1alpha1
kind: SealedSecret
metadata:
  name: cluster-autoscaler-hcloud
  namespace: kube-system
spec:
  encryptedData:
    HCLOUD_CLUSTER_CONFIG: AgPLACEHOLDER_RESEAL_ME_no_real_secret_here_0000000000
    HCLOUD_TOKEN: AgPLACEHOLDER_RESEAL_ME_no_real_secret_here_0000000000
  template:
    metadata:
      name: cluster-autoscaler-hcloud
      namespace: kube-system
