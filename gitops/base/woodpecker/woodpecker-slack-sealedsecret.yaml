# PLACEHOLDER SealedSecret — contains NO real secret.
# Reseal with seal.sh / kubeseal against your own sealing key before applying.
---
apiVersion: bitnami.com/v1alpha1
kind: SealedSecret
metadata:
  name: woodpecker-slack
  namespace: ci-build
spec:
  encryptedData:
    webhook: AgPLACEHOLDER_RESEAL_ME_no_real_secret_here_0000000000
  template:
    metadata:
      name: woodpecker-slack
      namespace: ci-build
