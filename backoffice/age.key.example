# SOPS age private key for the backoffice reconciler. Generate with `age-keygen`,
# put the PRIVATE key here (-> /etc/infra/age.key on the box) and the PUBLIC key
# (age1...) into .sops.yaml. Keep the real age.key OUT of git (gitignored).
# AGE-SECRET-KEY-...
